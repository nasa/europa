#include "Model.nddl"

HorizonFiltered h = new HorizonFiltered();

close();

// Now subgoal an uber predicate as rejectable. Expect that it will
// kick off a chain
rejectable(HorizonFiltered.uberPredicate t0);
t0.start.specify(0);