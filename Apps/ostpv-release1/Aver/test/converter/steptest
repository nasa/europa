Test('Steptest',
At first step : Count(Tokens()) == 1;
At last step : Count(Tokens()) == 1;
At any step : Count(Tokens()) == 1;
At each step : Count(Tokens()) == 1;
At step > 10 : Count(Tokens()) == 1;
At any step > 10 : Count(Tokens()) == 1;
At each step > 10 : Count(Tokens()) == 1;
At step in [0 3] : Count(Tokens()) == 1;
At any step in [0 3] : Count(Tokens()) == 1;
At each step in [0 3] : Count(Tokens()) == 1;
);
