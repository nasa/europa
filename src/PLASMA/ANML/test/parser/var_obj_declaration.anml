bool a := true;
bool b := flase, c := true;
int [0 +inf] d;
float [0.0 +inf] e := +inf, f := 54;
string g := "foo";

enum {3, 6, 4, 2} aEnum := 3;
vector (int i, float j, string k) aVector;

type Foo : float [0 +inf];
Foo m := 1.5;
