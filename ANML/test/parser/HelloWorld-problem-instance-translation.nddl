
PlannerConfig plannerConfiguration = new PlannerConfig(0, 100, 500, +inf);

YourObject object = new YourObject();
close(); // Close the the PLASMA Database - no more objects can be created.

// For each goal, we'd need to look for actions that
// can bring them about and generate a disjunction here
goal(YourObject.action_HelloWorld goal1);
leq(goal1.start,100);   
leq(100,goal1.end);   

// TODO: must generate this to prevent the goal from being clipped?
leq(0,goal1.start);   
