<EmptyFilters>
 <StaticFlawFilter/>
 <DynamicFlawFilter/>
</EmptyFilters>
<StaticMatching>
 <StaticFlawFilter>
  <-- Exclude all temporal variables -->
  <Filter scope="*.start"/>
  <Filter scope="*.end"/>
  <Filter scope="*.duration"/>

  <-- Exlude all arg1 parameters of predicateA on A and all its subclasses -->
  <Filter scope="A.predicateA.arg1"/>

  <-- Exclude all variables of predicateB -->
  <Filter scope="A.predicateB.*"/>

  <-- Exclude all tokens and variables of class D -->
  <Filter scope="D.*"/>

  <-- Exclude all object flaws on instances of PredicateRoot or its subclasses -->
  <Filter scope="PredicateRoot"/>

  <-- Exclude all token flaws in predicate C.PredicateC -->
  <Filter scope="E.PredicateC"/>

 </StaticFlawFilter>
</StaticMatching>