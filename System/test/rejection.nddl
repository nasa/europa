#include "NddlWorld.nddl"

class rejection {
  predicate unsatisfiable {
  }
}

rejection::unsatisfiable {
    eq(start, end);
}

NddlWorld world = new NddlWorld(0, 10, 100);
rejection r = new rejection();
any(rejection.unsatisfiable a);
a.start.specify(2);
