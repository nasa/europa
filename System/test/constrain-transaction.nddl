#include "NddlWorld.nddl"

class Foo {
  predicate bar { }
  predicate baz { }
}

Foo::bar {
}

Foo::baz {
}

NddlWorld world = new NddlWorld(0, 5, 10);
Foo f1 = new Foo();
Foo f2 = new Foo();
close();
any(Foo.bar t1);
any(Foo.baz t2);
t1.activate();
f2.constrain(t1);
t2.activate();
f2.constrain(t2, t1);
